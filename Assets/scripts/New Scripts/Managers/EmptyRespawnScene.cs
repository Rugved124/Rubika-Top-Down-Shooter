using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;
public class EmptyRespawnScene : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //SceneManager.MoveGameObjectToScene(FindObjectOfType<PC>().gameObject, SceneManager.GetSceneAt(GameManager.Instance.currentScene));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
